"""
Write a `find_gcd()` function that takes two numbers as arguments and returns the greatest common divisor of the
two numbers.
"""

import math


def find_gcd(a, b):
    return math.gcd(a, b)


print(find_gcd(12, 16))
